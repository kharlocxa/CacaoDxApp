{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "skipLibCheck": true   // ✅ don’t check types inside node_modules
  },
  "exclude": ["node_modules"] // ✅ completely ignore node_modules
}
